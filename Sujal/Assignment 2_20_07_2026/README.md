Compare the memory size of:

• A list containing one million numbers.
Answer:-The List will sTore all the one million numbers at once which will take a lot of memory 
• A generator containing one million numbers.
Answer:-While using the generator the generator only remebers and store the current element and wll return the output according to the demand of the user.