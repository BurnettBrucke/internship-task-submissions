# Calculate a salary after an 18 percent increment.

salary = float(input("Enter your current salary: "))
increment = salary * 18/100
new_salary = salary + increment
print("New salary after the 18% increment is:", new_salary)
